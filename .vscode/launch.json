{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run CodeX Assistant",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
